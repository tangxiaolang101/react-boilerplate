// import { fromJS } from 'immutable';
// import { makeSelectYubaHomepageDomain } from '../selectors';

// const selector = makeSelectYubaHomepageDomain();

describe('makeSelectYubaHomepageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
